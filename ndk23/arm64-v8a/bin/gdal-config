#!/bin/sh
CONFIG_LIBS="-LD:/Github/AndroidNativeKit/ndk23/arm64-v8a/lib -lgdal"
CONFIG_DEP_LIBS="-LD:/Github/AndroidNativeKit/ndk23/arm64-v8a/lib -lssl -lcrypto -ltiff -ljpeg -lpng -lgiflib -lwebp -lz -lbasisu -lzstd -lm -lsqlite3 -lexpat -lxml2 -lgeos_c -lcurl -lproj -pthread -lc++"
CONFIG_PREFIX="D:/Github/AndroidNativeKit/ndk23/arm64-v8a"
CONFIG_CFLAGS="-ID:/Github/AndroidNativeKit/ndk23/arm64-v8a/include"
CONFIG_DATA="D:/Github/AndroidNativeKit/ndk23/arm64-v8a/share/gdal"
CONFIG_VERSION="3.10.3"
CONFIG_GNM_ENABLED=yes
CONFIG_FORMATS="JPEG raw GTIFF MEM vrt Derived GTI HFA SDTS NITF GXF AAIGrid CEOS SAR_CEOS XPM DTED JDEM Envisat ELAS FIT L1B RS2 ILWIS RMF Leveller SGI SRTMHGT IDRISI GSG ERS PALSARJaxa DIMAP GFF COSAR PDS ADRG COASP TSX Terragen BLX MSGN TIL R northwood SAGA XYZ ESRIC HF2 KMLSUPEROVERLAY CTG ZMap NGSGEOID IRIS MAP CALS SAFE SENTINEL2 PRF MRF WMTS GRIB BMP TGA STACTA SNAP_TIFF BSB AIGrid USGSDEM AirSAR OZI PCIDSK SIGDEM RIK STACIT PDF PNG GIF WCS HTTP Zarr DAAS EEDA PLMOSAIC WMS OGCAPI WEBP Rasterlite MBTiles PCRaster BASISU_KTX2 MEM GeoJSON TAB Shape KML VRT AVC GML CSV DGN GMT NTF S57 Tiger Geoconcept GeoRSS DXF PGDump GPSBabel EDIGEO SXF OpenFileGDB WAsP Selafin JML VDV FlatGeobuf MapML MiraMon JSONFG SDTS GPX SVG CSW PLSCENES WFS OAPIF NGW Elastic Idrisi PDS SQLite GeoPackage OSM VFK MVT PMTiles AmigoCloud Carto XLSX CAD GTFS ODS LVBAG"
CONFIG_PLUGINDIR="D:/Github/AndroidNativeKit/ndk23/arm64-v8a/lib/gdalplugins"
usage()

{
	cat <<EOF
Usage: gdal-config [OPTIONS]
Options:
	[--prefix[=DIR]]
	[--libs]
	[--dep-libs]
	[--cflags]
	[--datadir]
	[--version]
	[--ogr-enabled]
	[--gnm-enabled]
	[--formats]
	[--plugindir]
EOF
	exit $1
}

if test $# -eq 0; then
	usage 1 1>&2
fi

case $1 in
  --libs)
    echo $CONFIG_LIBS
    ;;

  --dep-libs)
    echo $CONFIG_DEP_LIBS
    ;;

  --cflags)
    echo $CONFIG_CFLAGS
    ;;

  --datadir)
    echo $CONFIG_DATA
    ;;

  --prefix)
    echo $CONFIG_PREFIX
    ;;

  --version)
    echo $CONFIG_VERSION
    ;;

  --ogr-enabled)
    echo yes
    ;;

  --gnm-enabled)
    echo $CONFIG_GNM_ENABLED
    ;;

  --formats)
    echo $CONFIG_FORMATS
    ;;

  --plugindir)
    echo $CONFIG_PLUGINDIR
    ;;

  *)
    usage 1 1>&2
    ;;

esac
